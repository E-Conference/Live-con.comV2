<?xml version="1.0" ?>

<container xmlns="http://symfony.com/schema/dic/services"
    xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
    xsi:schemaLocation="http://symfony.com/schema/dic/services http://symfony.com/schema/dic/services/services-1.0.xsd">

    <parameters>
        <parameter key="serialize_exception_listener.class">fibe\RestBundle\Listener\SerializeExceptionListener</parameter>
        <!--<parameter key="app.cors_listener.class">fibe\RestBundle\Listener\CorsListener</parameter>-->
        <!--<parameter key="session.storage.native_file.class">fibe\RestBundle\Session\Storage</parameter>-->
    </parameters>

    <services>
        <service id="serialize_exception_listener" class="%serialize_exception_listener.class%">
            <tag name="kernel.event_listener" event="kernel.exception" method="onKernelException"/>
            <argument type="service" id="serializer" />
        </service>

        <!-- cors support -->

        <!-- <service id="app.cors_listener" class="%app.cors_listener.class%">
            <tag name="kernel.event_listener" event="kernel.response" method="onKernelResponse"/>
        </service> -->


        <!-- wsse authentication-->

        <service id="wsse.security.authentication.provider"
                 class="fibe\RestBundle\Security\Authentication\Provider\WsseProvider" public="false">
            <argument /> <!-- User Provider -->
            <argument>%kernel.cache_dir%/security/nonces</argument>
        </service>

        <service id="wsse.security.authentication.listener"
                 class="fibe\RestBundle\Security\Firewall\WsseListener" public="false">
            <argument type="service" id="security.context"/>
            <argument type="service" id="security.authentication.manager" />
        </service>
    </services>

</container>
