{% extends '::base.html.twig' %}

{% block body -%}
  <h1>Sponsor</h1>

  <table class="record_properties">
    <tbody>
    <tr>
      <th>Id</th>
      <td>{{ entity.id }}</td>
    </tr>
    <tr>
      <th>Label</th>
      <td>{{ entity.label }}</td>
    </tr>
    <tr>
      <th>Logopath</th>
      <td>{{ entity.logoPath }}</td>
    </tr>
    <tr>
      <th>Slug</th>
      <td>{{ entity.slug }}</td>
    </tr>
    </tbody>
  </table>

  <ul class="record_actions">
    <li>
      <a href="{{ path('sponsor') }}">
        Back to the list
      </a>
    </li>
    <li>
      <a href="{{ path('sponsor_edit', { 'id': entity.id }) }}">
        Edit
      </a>
    </li>
    <li>{{ form(delete_form) }}</li>
  </ul>
{% endblock %}
