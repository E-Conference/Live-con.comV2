{% extends '::base.html.twig' %}

{% block body -%}
    <h1>SocialServiceAccount</h1>

    <table class="record_properties">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ entity.id }}</td>
            </tr>
            <tr>
                <th>Accountname</th>
                <td>{{ entity.accountName }}</td>
            </tr>
            <tr>
                <th>Socialservice</th>
                <td>{{ entity.socialService }}</td>
            </tr>
        </tbody>
    </table>

        <ul class="record_actions">
    <li>
        <a href="{{ path('socialserviceaccount') }}">
            Back to the list
        </a>
    </li>
    <li>
        <a href="{{ path('socialserviceaccount_edit', { 'id': entity.id }) }}">
            Edit
        </a>
    </li>
    <li>{{ form(delete_form) }}</li>
</ul>
{% endblock %}
